#ifndef ASSIGN2_UTILS_H
#define ASSIGN2_UTILS_H

#include "Player.h"
#include "board.h"

class Utils {
   public:
    void updateScores(Player player, Board board, int rows, int cols);

   private:
}

#endif  //ASSIGN2_UTILS_H